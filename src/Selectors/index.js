/**
 * NOT IN USE
 * @param {*} state 
 * @returns 
 */
export const getUsers = state => state.users;

/**
 * Gets the current index variable in the Redux store
 * @param {*} state The state of the Redux store
 * @returns The value of the index variable contained in the Redux store
 */
export const getCurrentIndex = state => state.index;